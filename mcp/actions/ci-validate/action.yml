name: BoomTick CI Validate
description: Runs pre-submit checks via python3 -m dev_tools.cli CLI
runs:
  using: composite
  steps:
    - uses: arii/boomtick/mcp/actions/setup@actions-v1
    - name: Pre-submit gate
      run: python3 -m dev_tools.cli gh pre-submit
      shell: bash
