{
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "type-check": "pnpm --filter @boomtick/mcp run type-check",
    "lint": "echo 'Skipping lint for now'",
    "test": "pnpm --filter @boomtick/mcp run test"
  }
}
