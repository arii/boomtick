name: CI Validate

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  packages: read

jobs:
  validate:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/${{ github.repository }}/ai-workspace-runner:latest
      credentials:
        username: ${{ github.repository_owner }}
        password: ${{ secrets.GITHUB_TOKEN }}
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      ALLOW_NODE_VERSION_CHANGE: "true"
      TD_BASE_BRANCH: ${{ github.base_ref || 'main' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch base branch
        run: |
          BASE_BRANCH="${{ github.base_ref || 'main' }}"
          git fetch origin "$BASE_BRANCH:$BASE_BRANCH" || git fetch origin "$BASE_BRANCH"

      - name: Validate Environment Parity
        run: |
          node --version
          pnpm --version
          python3 --version

      - name: Bootstrap Agent Workspace
        run: ./setup-agent.sh

      - name: BoomTick CI Validate
        uses: ./boomtick-pkg/mcp/actions/ci-validate
